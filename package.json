{
  "name": "ironlog",
  "version": "1.0.0",
  "description": "IronLog - a simple full-stack gym tracking web app",
  "type": "commonjs",
  "scripts": {
    "start": "NODE_ENV=production node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "pg": "^8.22.0"
  }
}
